@{
    ViewData["Title"] = "Lockout";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow text-center">
                <div class="card-body p-5">
                    <i class="bi bi-lock text-warning" style="font-size: 5rem;"></i>
                    <h2 class="text-warning mt-3">Cuenta Bloqueada</h2>
                    <p class="text-muted">
                        Tu cuenta ha sido bloqueada temporalmente debido a múltiples intentos fallidos de inicio de sesión.
                    </p>
                    <p class="text-muted">
                        Por favor, intenta nuevamente en unos minutos o contacta al administrador.
                    </p>
                    <a href="@Url.Action("Login", "Account")" class="btn btn-primary mt-3">
                        <i class="bi bi-arrow-left"></i> Volver al Login
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>